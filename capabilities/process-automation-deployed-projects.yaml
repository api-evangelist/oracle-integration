naftiko: 1.0.0-alpha2
info:
  label: Oracle Integration Process Automation API — Deployed Projects
  description: 'Oracle Integration Process Automation API — Deployed Projects. 1 operations. Lead operation: Oracle Integration
    List QuickStart Apps. Self-contained Naftiko capability covering one Oracle Integration business surface.'
  tags:
  - Oracle Integration
  - Deployed Projects
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ORACLE_INTEGRATION_API_KEY: ORACLE_INTEGRATION_API_KEY
capability:
  consumes:
  - type: http
    namespace: process-automation-deployed-projects
    baseUri: https://{instance}.integration.ocp.oraclecloud.com
    description: Oracle Integration Process Automation API — Deployed Projects business capability. Self-contained, no shared
      references.
    resources:
    - name: ic-api-process-v1-quickStartApps
      path: /ic/api/process/v1/quickStartApps
      operations:
      - name: listquickstartapps
        method: GET
        description: Oracle Integration List QuickStart Apps
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.ORACLE_INTEGRATION_API_KEY}}'
  exposes:
  - type: rest
    namespace: process-automation-deployed-projects-rest
    port: 8080
    description: REST adapter for Oracle Integration Process Automation API — Deployed Projects. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/ic/api/process/v1/quickstartapps
      name: ic-api-process-v1-quickstartapps
      description: REST surface for ic-api-process-v1-quickStartApps.
      operations:
      - method: GET
        name: listquickstartapps
        description: Oracle Integration List QuickStart Apps
        call: process-automation-deployed-projects.listquickstartapps
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: process-automation-deployed-projects-mcp
    port: 9090
    transport: http
    description: MCP adapter for Oracle Integration Process Automation API — Deployed Projects. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: oracle-integration-list-quickstart-apps
      description: Oracle Integration List QuickStart Apps
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: process-automation-deployed-projects.listquickstartapps
      outputParameters:
      - type: object
        mapping: $.
